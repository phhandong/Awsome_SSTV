# 交互式音频播放器使用指南

## 📱 新增功能

原有的简单起始时间输入框已经升级为**完整的交互式音频播放器**，支持：

### ✨ 主要特性

1. **可视化波形显示**
   - 实时显示音频波形
   - 清晰的视觉反馈

2. **精确选区控制**
   - 拖动选区边缘调整起始/结束位置
   - 拖动选区中间整体移动
   - 实时显示选中的时间范围

3. **实时音频预览**
   - 播放/暂停控制
   - 仅播放选中区域
   - 实时播放进度指示

4. **时间显示**
   - 当前播放时间
   - 总时长
   - 选区起始/结束时间（秒）

## 🎮 使用方法

### 1. 加载音频文件
- 拖放 WAV/MP3 文件到上传区
- 或点击上传区选择文件
- 音频加载后会自动显示波形和播放器

### 2. 选择解码区域

**方法一：拖动选区边缘**
- 鼠标悬停在选区左/右边缘（会变色）
- 按住鼠标左键拖动调整起始/结束位置
- 光标会变为 ↔️ 调整大小样式

**方法二：移动整个选区**
- 点击选区中间（避开边缘）
- 按住拖动整体移动选区
- 光标会变为 ✋ 移动样式

**方法三：点击时间轴**
- 点击波形任意位置
- 播放头会跳转到该位置

### 3. 播放预览
- 点击播放按钮 ▶️ 播放选中区域
- 再次点击 ⏸ 暂停
- 红色播放头实时显示当前位置

### 4. 解码选中区域
- 调整好选区后点击"解码"按钮
- 系统会自动使用选中的起始和结束时间
- Toast 提示会显示解码的具体时间范围

### 5. 重置选区
- 点击"重置选区"按钮
- 选区恢复为完整音频范围（0% ~ 100%）

## 🎨 视觉元素说明

| 元素 | 说明 |
|------|------|
| 🟦 青色选区 | 将要解码的音频范围 |
| 🔵 青色边缘 | 可拖动调整起始/结束位置 |
| 🔴 红色播放头 | 当前播放位置 |
| 🟩 青色波形 | 音频信号强度 |
| ⚫ 黑色背景 | 时间轴画布 |

## ⌨️ 键盘快捷键

- `Space` - 播放/暂停（焦点不在输入框时）
- `Ctrl/Cmd + D` - 解码选中区域
- 点击波形区域 - 跳转到该时间点

## 📐 技术细节

### 时间精度
- 选区时间显示精度：0.1 秒
- 实际解码精度：采样点级别（取决于采样率）

### 选区限制
- 最小选区宽度：1% 总时长
- 防止选区边缘交叉
- 自动边界限制（0% ~ 100%）

### 性能优化
- Canvas 硬件加速渲染
- requestAnimationFrame 平滑更新
- 防抖优化拖动性能

## 🎯 使用场景示例

### 场景 1：跳过前置静音
1. 加载包含前置静音的 SSTV 音频
2. 观察波形，找到信号开始位置
3. 拖动选区左边缘到信号起点
4. 点击解码

### 场景 2：解码多张图片
1. 加载包含多个 SSTV 信号的长音频
2. 选择第一张图片的区域并解码
3. 移动选区到第二张图片位置
4. 再次解码

### 场景 3：精确定位 VIS 码
1. 播放音频预览
2. 听到 VIS 码起始音时暂停
3. 从该位置开始选区
4. 解码完整信号

## 🔧 故障排除

### 播放器未显示
- 确认已成功上传音频文件
- 检查浏览器控制台是否有错误
- 尝试刷新页面重新上传

### 无法播放音频
- 检查浏览器是否支持 Web Audio API
- 确认未静音
- 尝试点击页面其他位置（某些浏览器需要用户交互）

### 拖动不流畅
- 关闭浏览器其他标签页释放内存
- 对于超长音频（>5 分钟），可能会有轻微延迟

## 🌐 浏览器兼容性

| 浏览器 | 版本 | 支持情况 |
|--------|------|----------|
| Chrome | 90+ | ✅ 完全支持 |
| Firefox | 88+ | ✅ 完全支持 |
| Safari | 14+ | ✅ 完全支持 |
| Edge | 90+ | ✅ 完全支持 |
| 移动浏览器 | 现代版本 | ✅ 支持（触摸拖动） |

## 💡 提示

1. **缩放波形**：使用浏览器缩放功能（Ctrl + 滚轮）可以更精确地查看波形细节
2. **选区对齐**：点击波形可以快速定位到特定时间点
3. **快速测试**：先播放预览确认选区正确，再执行解码
4. **批量处理**：记录每张图片的时间点，可以快速批量解码

---

**更新日期**: 2026-08-06  
**版本**: v2.0 - 交互式音频播放器
